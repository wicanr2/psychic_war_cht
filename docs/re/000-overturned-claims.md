# 000：已被推翻的斷言

`docs/re/` 正文只寫現況。被後來的證據推翻的斷言集中記在這裡，正文不保留推翻過程。

| 日期 | 原斷言（出處） | 推翻的證據 | 現況在哪 |
|---|---|---|---|
| 2026-09-17 | 敵人的攻擊綁在計時器上；CPU 越快敵人越來不及出手，速度影響難度（`009` §5.1–5.2） | 戰鬥主迴圈每圈都判定敵人攻擊、不讀計時器；同一按住起點在 240／750 cycles 下 8/12 組損血相同。原推論只比了 DOSBox-X 一場（玩家掉血）與 dosgolem 一場（沒掉），而單場結果由亂數與按鍵時機決定 | `010` §3、§4.2 |
| 2026-09-17 | DOSBox 的 cycle 就是指令數（dosgolem 規格 198 初版 §2） | DOSBox-X `core_normal/string.h` 字串指令每次迭代多扣一個 cycle | `010` §2、dosgolem `198` |
| 2026-09-17 | 玩家 HP 與能量的位址找不到（`009` §3、§7） | 玩家 HP `0x16990`、能量 `0x509E`／`0x16994` 改值驗證通過 | `011` |

教訓（規則形式）：
- 拿單場戰鬥的結果推時間基準之前，先在同一速度下改變按鍵時機，看結果本身的變異有多大。
- 拿別的模擬器的單位（cycles）設定速度之前，先讀它的原始碼怎麼扣。
