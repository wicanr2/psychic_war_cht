# 第 10 輪：README 與當年攻略

## 目標

**把 OPL2 合成器最後一段差距找出來，並把專案對外的門面補齊：README（含中文截圖）與當年的攻略。**

第 9 輪之後，聲音**做完了**——OPL2 的三項指標全部過門檻，真實裝置上的欠載也量到了；
輸入完整（名字、滑鼠、錄放），授權放進 repo。這一輪是**別人第一眼看到的東西**：
README 與當年的攻略。

`#5` 依使用者定案 2026-09-19 收在「標題曲通過」，迷宮曲不驗。

## 使用者定案（沿用，不重問）

- #8「從開頭跑到結局」不執行；驗收用抽測。
- 輸入名字只支援 ASCII。
- 授權採 RRSAL-1.0。
- 字型用倚天字形發行；dosgolem 的修改整個移植完成後一次發 PR（分支 `psychic-war/r5-text`）。

## 起點（第 9 輪留下的東西）

| 產物 | 位置 |
|---|---|
| OPL2 保真度判準與現況 | `docs/spec/016`（READY）、`docs/re/032`、`tools/opl_wav_compare.py` |
| 合成器（含 KSL、震音、顫音、指數起音、診斷開關） | dosgolem `196` 修訂、`internal/opl2` |
| 輸入名字、滑鼠、輸入錄放 | `docs/spec/017`／`018`／`019` ＋ 三支驗收腳本 |
| LICENSE（RRSAL-1.0） | `LICENSE` |
| 圖檔文字 45／50 張 | `text/baked.json`、`docs/re/031` |

## 本輪完成條件

1. **README（#37）**
   - 遊戲介紹、中文截圖、怎麼玩、熱鍵表、進度、授權與字型來源（含下架聯絡方式）。
   - **先決定**：截圖裡有原版美術，README 放截圖算不算散布原版素材（本 repo 的 `[HARD]` 規則）。
     這件事要先問過再動手。
   - 依 `rulebook/80`（retro 中文化 README）、`rulebook/90`／`91`（白話、去 AI 味）寫。
2. **當年攻略整理（#38）**
   - 《軟體世界》19 期 29–38 頁，10 頁掃描逐頁判讀，整理成 markdown 放 `docs/`。
   - **掃描圖不進 repo**（原版素材）；內容改寫不逐字轉錄，標明出處。
   - 同時當譯名的校對材料：攻略裡的人名、地名、超能力名稱與 `text/glossary.json` 對齊，
     不一致的列出來處理。
3. ~~即時音訊（#13）~~ **第 9 輪做完了**：真實裝置上 AdLib 與 PC 喇叭各 65 秒，
   欠載都是 6 次全在開場第 1 秒、第 2 秒之後 0（`docs/re/026` §6）。
4. `tools/py.sh tools/worklist.py verify` 沒有「可能已完成」；做完的條目移到 `done`；本機 commit。

## 延後

- 疊字的挖空模式與 `OPEN.PBL` #7–#9（#25 收尾）：畫面上剩的英文只有那 3 張字幕條與
  2 張定案保留的標題美術字。
- #9 節拍：5 場有效樣本已取得，差的是低負載下的複量。
- #34 主題、#35 跨平台打包。

## 量測指標

- README：有沒有截圖、章節是否齊全（過一遍 `rulebook/91` 的反面清單）。
- 攻略：整理了幾頁、譯名對齊了幾條、不一致幾條。
- 音訊：60 秒欠載次數、量測當時的每核負載。

## 工作邊界

- SDD：改 dosgolem 前先寫規格標 READY；每次改 dosgolem 跑全套測試。
- 全部走 docker（Python 用 `tools/py.sh`，**子代理也一樣**）；只清理自己建立的 container，禁止 prune／`rmi`。
- 原版素材只放 `workplace/` 與 private repo 的 `text/`。
- **push、PR 先問**；issue 狀態與留言可以直接更新。

## 收尾

- 每項完成：證據進 `docs/re/` 或 `docs/spec/`，worklist 移到 `done` 或更新現況，`render`，本機 commit。
- 在對應 issue 留言進度；完成的關閉。
- 回報附數字；產生下一輪的 goal markdown。
